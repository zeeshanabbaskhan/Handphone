{
  "name": "handphone",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "build": "node scripts/build-client.js",
    "start": "cross-env NODE_ENV=production npm start --prefix server",
    "dev": "cross-env NODE_ENV=development node server/index.js",
    "vercel-build": "node scripts/build-client.js"
  },
  "devDependencies": {
    "cross-env": "^7.0.3"
  }
}
